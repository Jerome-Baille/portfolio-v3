const emailURL = 'https://email-server.jerome-baille.fr/api/email/jerome-portfolio';
const portfolioURL = 'https://jerome-baille.fr/api';

export const environment = {
  production: true,
  emailURL,
  portfolioURL,
  aboutURL: `${portfolioURL}/about`,
  projectURL: `${portfolioURL}/projects`
};